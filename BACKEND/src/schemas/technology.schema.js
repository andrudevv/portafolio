const Joi = require("joi");

const createTechnologyShema = Joi.object({
 
})

const updateTechnologyShema = Joi.object({
 
  
});


const getTechnologyShema = Joi.object({
 
});


module.exports ={ createTechnologyShema, updateTechnologyShema, getTechnologyShema };
